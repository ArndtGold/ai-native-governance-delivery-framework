# AGDF Context Graph

## Active Context Nodes

### CG-PUBLIC-PLUGIN-DISTRIBUTION

- situation: AGDF needs a public OpenAI directory distribution whose repository candidate, shared ChatGPT/Codex capability claims, verified-publisher prerequisite, portal lifecycle and effective listing state cannot be represented honestly as one package or release flag.
- refs: plugin/meta/agdf-plugin.definition.json; plugin/.codex-plugin/plugin.json; create-agdf/scripts/sync-package-assets.js; create-agdf/scripts/sync-plugin-runtime.js; .github/workflows/publish-agdf.yml; PRIVACY.md; TERMS.md; SUPPORT.md; SECURITY.md; .agdf/control/artefacts/agdf-public-plugin-distribution/PRD.md; .agdf/control/artefacts/agdf-public-plugin-distribution/SD.md
- evidence: Approved UR, PRD Revision 1 and SD Revision 1, post-UR Brownfield Review and UX Intent Definition identify existing canonical plugin/package/Pages/release owners, a prior shipped-runtime metadata gap, high public-contract and cross-host impact, and current official OpenAI submission requirements inspected on 2026-08-17 and revalidated on 2026-08-18. TP planning measured a direct conflict between final public-submission limits (30-character display name, 30-character short description, at most three 128-character prompts) and the established local Codex interface. Package validation permits the existing longer local values. Publisher identity is currently unverified.
- decision: One skills-first/no-MCP public candidate is generated deterministically from canonical AGDF sources. The ordinary local Codex manifest preserves `AI Governance & Delivery Framework`, local/package description `Control layer for governed AI-assisted delivery.`, four prompts and hooks. Only the public candidate uses `AGDF`, `Governed AI delivery controls`, three compliant prompts and no hooks; long-form public copy and the website retain `AI Governance & Delivery Framework (AGDF)`. Both projections come from one canonical definition and one projector, while one strategic `longDescription` serves Codex, Claude and public submission. Portable skills are shared; Codex hooks are surface-specific and trust-gated. Repository, exact-bundle, installed-host, portal and post-publication evidence remain separate. OpenAI owns verified identity, review and effective directory state; every portal mutation requires explicit human authorization and read-back.
- invariants: generated submission material never becomes a second gate, skill, metadata or policy owner; local package identity and public listing identity remain explicit separate projections; local/package short copy has one owner (`description`) and detailed product positioning has one owner (`longDescription`) shared by Codex, Claude and public submission, with no host- or public-specific duplicate long-copy fields; constrained public display, short copy and prompts are deliberately scoped and tested against final-submission limits, never silently applied to the ordinary local manifest or truncated; the shared positioning distinguishes the general governance framework from its first governed software-delivery implementation and does not claim broader implemented domains; installation and identity verification never approve AGDF gates or prove repository readiness; unsupported surface behavior is advisory, unavailable or unverified; final-bundle validation resolves every declared runtime path; portal automation is absent; Persona inquiry URLs, identity documents, images, tokens and session material are never persisted; npm release and OpenAI publication remain independent operations.
- risks: OpenAI submission formats, field limits and host capabilities may drift; constrained public identity can diverge from the full product name; public copy can overstate enforcement; package manifests can reference absent payload files; canonical repository and portal copy can diverge; verified identity or Apps Management authority may remain unavailable. Controls are current-doc revalidation, explicit upstream product decisions, deterministic projection/inventory proof, exact-release host UAT, explicit evidence classes and fail-closed external transitions.
- exit_criteria: canonical public metadata, policy URLs, reviewer cases, capability matrix and deterministic candidate pass repository and exact-bundle validation; supported Codex and applicable ChatGPT behavior has direct exact-release evidence; publisher authority and portal states are read back without sensitive identity material; publication, if separately authorized, is discoverable and post-publication claims are verified or visibly limited.

### CG-PUBLIC-COMMUNITY-GOVERNANCE

- situation: AGDF previously exposed README and License but lacked a recognized conduct, contribution, security, support, issue/PR routing and maintainer-governance surface. GitHub-hosted metadata and security capabilities also have a different mutation and evidence path from committed files.
- refs: CODE_OF_CONDUCT.md; CONTRIBUTING.md; SECURITY.md; SUPPORT.md; GOVERNANCE.md; .github/ISSUE_TEMPLATE/; .github/pull_request_template.md; .github/CODEOWNERS; .github/repository-metadata.json; .agdf/control/artefacts/github-community-health-governance/SD.md
- evidence: Approved UR, PRD, SD and TP plus pre-implementation Brownfield Analysis identify five canonical root policies, thin GitHub adapters, one desired-state metadata manifest and separate live-host recognition evidence.
- decision: Root policy files are the durable public contract. `SECURITY.md`, `SUPPORT.md`, `CONTRIBUTING.md`, `GOVERNANCE.md` and `CODE_OF_CONDUCT.md` use canonical English copy for public review and international routing while explicitly accepting participation in English or German and preserving German-primary governance; no independently maintained translation fork exists. GitHub templates and CODEOWNERS project that contract into host interactions. `.github/repository-metadata.json` owns desired settings while GitHub remains the effective host state. Repository checks and authenticated/default-branch observations are reported separately.
- invariants: confidential security reporting always has `agdf@iself.eu` as a complete fallback; sensitive details are never requested publicly; security support is current-release-line and best-effort without numeric SLA; German is primary and English submissions are accepted; Arndt Gold is the sole current maintainer until a reviewed governance and CODEOWNERS change says otherwise; CODEOWNERS routes review but does not prove branch protection; adapters link to canonical policies instead of duplicating them; local files never prove applied GitHub settings or Community Profile recognition.
- risks: GitHub Issue Forms and settings behavior can drift; a sole-maintainer model concentrates availability; desired and effective metadata can diverge; default-branch recognition is unavailable before delivery. These are controlled by dependency-pinned validation, fail-safe security routing, explicit succession, before/after host read-back and post-delivery verification.
- exit_criteria: deterministic community-health checks, repository regression tests and mandatory reviews pass; host-applied metadata and security state are observed separately; after authorized default-branch delivery, Community Profile, Issue Forms, PR template, CODEOWNERS, links and social preview are visibly recognized.

### CG-RUN-SCOPED-CONTROL-STATE

- situation: A global mutable `AGDF_RUN.md` creates cross-run conflicts for concurrent users, machines and agent sessions.
- refs: .agdf/control/artefacts/agdf-run-scoped-control-state/PRD.md; .agdf/control/artefacts/agdf-run-scoped-control-state/SD.md; create-agdf/lib/control-state/
- evidence: Approved PRD and SD assign one canonical `RUN_STATE.md` per run and one shared resolver; focused control-state tests pass. 2026-07-13 UAT-preparation delta 1: live `delivery-map --all-active` surfaced `AGDF_LEGACY_PROJECTION_DRIFT`, traced to `fsyncDirectory()` in `create-agdf/lib/control-state/run-state-writer.js` throwing `EPERM` on native Windows (no platform guard existed), which meant every canonical write failed outright on that platform. Fixed with a single-line `win32` guard; re-verified via `delivery-map --all-active` (block → pass, 0 findings) and the full `test:control-state` suite (previously unable to complete on this platform, now passes). A second, unrelated pre-existing gap in `create-agdf/scripts/control-state-test.js` — a symlink-creation fixture aborting the entire test process without Windows Developer Mode/elevation — was fixed by skipping only its two dependent assertions with a logged warning on `EPERM`, leaving production symlink-rejection logic untouched. 2026-07-13 UAT-preparation delta 2: a live UAT demonstration with a full scaffold and two active runs found that `doctor`/`gate-check`/`delivery-map` (single-run path, no `--run`/`--all-active`) crashed with an uncaught `AGDF_ACTIVE_RUN_AMBIGUOUS` exception and a raw stack trace instead of a structured `--json` finding. Root cause: `readRunState()` called `resolveRuns()` with no try/catch, and none of the three CLI evaluators caught it either. Fixed at the single root (`readRunState` returns a `resolution_error` instead of throwing; `evaluateDoctor` turns it into one `block` finding), with a permanent regression test added covering all three commands.
- decision: Mutable authority is isolated per run; discovery is derived; ambiguity and migration are explicit; legacy projections are non-authoritative and never updated automatically.
- invariants: one mutable owner per run; no writable global active index; no automatic migration; no `merge=union` on canonical records; all consumers share selection semantics; directory-fsync (as used by `atomicWrite`'s durability step) is not supported by Windows at the OS level and must be explicitly skipped on `win32`, not retried or blanket-caught; test fixtures that depend on creating a symlink must catch `EPERM` and skip only the dependent assertions with a logged reason instead of aborting the whole suite, since symlink creation requires elevated privileges or Developer Mode on Windows; every CLI entry point that resolves a run (`doctor`, `gate-check`, `delivery-map`, and any future consumer) must treat the shared resolver's typed errors (`AGDF_ACTIVE_RUN_AMBIGUOUS`, `AGDF_SELECTOR_CONFLICT`, `AGDF_RUN_NOT_SELECTABLE`) as structured findings, never as uncaught exceptions that crash the process.
- risks: mixed-version consumers and incomplete runtime integration remain open until TP completion. PRD/SD never stated an explicit Windows/cross-platform acceptance criterion, which is how the directory-fsync gap reached UAT preparation undetected; the invariant above is now durable so a future change does not silently reintroduce a POSIX-only assumption in this module. The ambiguous-run crash likewise reached UAT preparation undetected because no existing test exercised the CLI (not just the library) with more than one active run and no selector; a permanent regression test now closes that gap.
- exit_criteria: doctor, gate-check, delivery-map, Delivery Path Search, CI, packages and docs consume the shared resolver and all required migration/concurrency tests pass; cross-platform (including native Windows) write-path execution is part of that evidence going forward, not only POSIX/CI-runner execution; any new CLI entry point that resolves a run is added to the regression test's coverage of graceful ambiguous-selection handling.

### CG-RUN-STATUS-CARD

- situation: AGDF has machine-readable gate and delivery-map outputs, but needs a compact human/agent status projection that includes the next permissible step and the next meaningful quality outlook.
- refs: plugin/meta/contracts/interaction.md; plugin/meta/contracts/control-scaffold.md; create-agdf/bin/create-agdf.js; plugin/control/templates/AGDF_RUN.md; plugin/control/templates/artefacts/OR.md
- evidence: Existing fields include `next_allowed_action` and `quality_outlook`; the approved UR narrows this run to making the projection explicit and machine-readable.
- exit_criteria: Runtime contract, CLI JSON/text output, templates and smoke tests consistently expose the Run Status Card without creating a second gate model.
- lifecycle_extension_2026_07_16: General lifecycle status composes installation, repository activation and delivery as independent sections. A healthy installation and blocked delivery are intentionally valid together; delivery continues to derive from doctor/gate-check and lifecycle status never becomes a second gate evaluator. Fresh read-only request orientation reuses this projection, emits once and creates no run.
- rationale_extension_2026_07_16: The compact default projection stays concise; deterministic localized gate and internal-step rationale is retrieved only through the non-authorizing on-demand `Why?` status interaction. The same gate and locale produce the same curated explanation without adding approval controls or changing gate state.
- state_orientation_extension_2026_07_17: The compact human projection derives a breadcrumb from the selected Mode/Slice Decision and approvals, emits one separate post-acceptance transition narration and collapses internal sub-states to stable human labels. Machine/audit output remains unchanged; missing or unknown derivation inputs fail closed instead of selecting a fixed path template.

### CG-DELIVERY-PATH-SEARCH

- situation: High-impact AGDF delivery decisions can have several plausible next paths, so agents need a read-only way to compare delivery paths before implementation without turning search output into gate authority.
- refs: plugin/meta/contracts/control-scaffold.md; plugin/meta/contracts/gate-transition.md; plugin/meta/agdf-plugin.definition.json; plugin/meta/agdf-agent-router.md; plugin/skills/delivery-path-search/SKILL.md; create-agdf/lib/delivery-path-search/; create-agdf/bin/create-agdf.js; .agdf/control/artefacts/agdf-delivery-path-search/OR.md
- evidence: UAT-approved `agdf-delivery-path-search` delivered bounded best-first advisory search and deterministic gate legality. `claude-evaluator-tool-enforcement-implementation` added a tool-enforced Claude evaluator. `delivery-path-search-ai-candidate-generation` adds opt-in bounded candidate generation: deterministic contracts/policy/guard/orchestration/package tests pass; a real Codex generator probe returned three schema-valid proposals in 19.117s with one abstract cost unit and zero worktree mutation; a real Claude generator-path probe returned two schema-valid proposals in 25.309s with cost_units=2 and zero worktree mutation via `claude-haiku-4-5-20251001`. Two earlier real Claude generator-path attempts (default model, then Haiku) exceeded the approved 30000ms budget and were correctly terminated by the read-only guard with zero worktree mutation both times, before the third attempt succeeded; deterministic Claude adapter/auth/guard tests and the deterministic fallback path pass. `opencode-surface-hardening-parity` adds a conforming OpenCode evaluator with invocation-scoped deny preflight, contract validation and mutation guard; deterministic evidence passes, while the available live provider returned HTTP 401 before a contract-valid evaluation, so live OpenCode `tool_enforced` availability remains unclaimed.
- decision: Delivery Path Search remains advisory evidence only. Optional AI-native proposals supplement and never replace the deterministic baseline; canonical AGDF gates remain sole execution authority.
- invariants: bounded best-first search, not MCTS; at most one opt-in generation call, five proposals, 30 seconds and five abstract cost units within whole-run budgets; the core alone owns schema, exact `gate_action` legality, context allowlisting, duplicate/material-diversity rejection, budgets and visible fallback; provider adapters own transport only; automatic provider fallback is forbidden; Codex and Claude transports are designed as tool-enforced; OpenCode remains `instruction_only` by default and may report `tool_enforced` only for the current invocation after successful deny preflight and a completed contract-valid evaluator result with zero mutation; Copilot and generic remain instruction-only.
- risks: model scores and generator cost units are judgements, not provider-currency measurements; deterministic similarity is conservative; external generation adds privacy/cost/latency exposure bounded by allowlists and hard limits; the fixed 30000ms per-call generator budget is marginal for real Claude latency (two of three real generator-path attempts timed out at the cap before one succeeded), mitigated by the tested deterministic fallback and carried forward as a candidate SD-level follow-up; instruction-only surfaces cannot prove write prevention.
- claude_enforcement_finding: Verified against official Claude Code CLI docs (code.claude.com, 2026-07-09) and then live-tested against the actually-installed CLI (2.1.203) that `--disallowedTools "Edit,Write,Bash"` (headless `-p`/`--print` invocation) is enforced by Claude Code itself, not the model — confirmed live: a prompt explicitly asking to call the Write tool was refused because the tool was not offered, no file was created. Combined with the same `git status --porcelain` before/after mutation check already used in `evaluators/codex.js`, this is a legitimate `tool_enforced`-equivalent evidence path, cross-platform including Windows (unlike Claude's Bash-specific `--sandbox` flag, which lacks native Windows support and does not cover the file tools). Implemented in `evaluators/claude.js`, wired into `bin/create-agdf.js`'s `--surface claude`, and `capabilities.js` now reports `tool_enforced` for Claude with real evidence.
- exit_criteria: Runtime contract, CLI behavior, canonical skill routing, Pages/docs and package smoke tests consistently preserve advisory-only gates, deterministic baseline, generator boundaries, explicit surface capability claims and non-MCTS terminology; the authenticated Claude generator-path runtime caveat is closed by the successful in-budget probe, with the residual budget-marginality risk carried forward as a candidate SD-level follow-up.
- structured_depth_extension_2026_07_29: Delivery-path candidates consume the sole `Structured Depth Decision` in `plugin/meta/contracts/modes.md`. A positive `structured_slice` requires complete facts and all seven bounded-slice checks; any evidenced full-depth trigger selects `structured_delivery`; missing or conflicting decisive facts persist the existing `block` path with owner-oriented recovery. Candidate search remains advisory and cannot authorize, redefine or benchmark-tune this boundary.

### CG-DOCUMENTATION-CEREMONY-BOUNDARY

- situation: `AGDF_RUN.md` was a single, one-size-fits-all template applied regardless of change size; the Runtime Contract already defined a lightweight "Quick Task Output" shape but had no explicit, mechanically checkable criterion for when it could replace the full file. This was discovered directly in this session: even a prior pure-documentation quick task (`agdf-backlog-vocabulary-visibility`) produced the full ~14-section file.
- refs: plugin/meta/contracts/modes.md (`Quick Task Output`); plugin/meta/contracts/closeout.md (`Relevant Run`); plugin/control/templates/AGDF_RUN.md; create-agdf/bin/create-agdf.js (`liveControlFiles`, doctor)
- evidence: `agdf-micro-tier-below-quick-task` UR → Brownfield Review → PRD → SD → TP → Brownfield Analysis, all `pass`/approved on 2026-07-10. Confirmed `doctor` requires `AGDF_RUN.md` to exist (`AGDF_CONTROL_FILE_MISSING` block check) but does not parse or depend on the Runtime Contract's own prose, so the boundary could be added without touching `doctor`, any skill, or any code file. `agdf-self-maintenance-overhead-reduction` (2026-07-13) added a second, narrower extension: a four-condition "Narrow Code-Fix Criterion" for defect fixes that do touch an otherwise-excluded code path. Its wording was worked-evaluated against two real fixes from the same session's `agdf-run-scoped-control-state` run: a Windows `fsyncDirectory` guard (would not have qualified when first made, since its regression test did not yet exist) and a CLI ambiguous-selection crash fix (a genuine borderline case — its diff spanned one function plus its direct caller, which showed the initial "single function" wording would have wrongly excluded exactly the kind of fix the criterion is meant to cover). The wording was corrected to "single function, or a function together with its direct, necessarily-coupled caller" based on that concrete test before being adopted.
- decision: A `quick_task` whose entire diff stays fully outside `plugin/skills/**`, `plugin/control/templates/**`, `plugin/meta/**`, `create-agdf/lib/**`, `create-agdf/bin/**` and any other code file may close with only the compact Quick Task Output shape. It must not create, rewrite or expand any of `AGDF_RUN.md`'s core sections. It gets a `MASTER_BACKLOG.md` entry only if otherwise a "Relevant Run," and at most one appended `Prior Run Pointers` line if `AGDF_RUN.md` currently reflects a different, unrelated run. Separately, a change that does touch one of those otherwise-excluded code paths may still use the compact shape (Code Review still mandatory) if it meets the four-condition Narrow Code-Fix Criterion in `plugin/meta/contracts/modes.md`'s Trivial Change Boundary subsection. A third `verified_change` mode may handle a bounded, user-visible canonical metadata/copy change after approved UR and Brownfield Review only through `VERIFIED_CHANGE.md`: it names one canonical owner, clean-at-baseline source/derived paths, prohibited-impact absence, deterministic propagation/validation and a fixed structured escalation target; `doctor` and `gate-check` fail closed if any condition or post-baseline scope check is missing, false or ambiguous.
- invariants: the boundary is an explicit path-based allow-list, not a prose judgment call, and fails closed (unlisted or ambiguous paths keep full ceremony); ceremony for anything touching the listed normative paths is unchanged except an eligible `verified_change` record; the canonical Runtime Contract owns its lifecycle and transition model. The Narrow Code-Fix Criterion is likewise an explicit, fail-closed four-condition list (single bounded diff; accompanying regression test; no product/gate-semantics change; `doctor`/tests unchanged in shape) — any single condition failing or any ambiguity keeps full ceremony; it never allows a fix to close with zero test evidence, and Code Review is never skippable under it. A consolidated Verified Change may own Brownfield selection, execution and Mini-Closeout only through lifecycle-consistent same-record roles; active scope uses live post-baseline evidence, while completed history uses the persisted exact changed-path snapshot and never treats missing historical evidence as pass.
- risks: the allow-list could miss a future normative location added under a new subdirectory — mitigated by the fail-closed default rather than assumed coverage; a prose-only version of this boundary would have been a scope-creep loophole, which is why it was rejected during Brownfield Review/PRD in favor of the explicit path list. The same scope-creep-loophole risk applies to Verified Change and is mitigated by its durable record, candidate-clean baseline and automatic structured escalation; unrelated pre-existing dirty paths are isolated by the baseline rather than treated as permission or failure.
- exit_criteria: Runtime Contract wording, generated Codex/Copilot/OpenCode surface copies (Claude reads `plugin/` directly, no separate copy) and a genuine worked example (this run's own `README.md` `agdf/` directory-listing fix; and `agdf-self-maintenance-overhead-reduction`'s own `MASTER_BACKLOG.md` `[framework-maintenance]` tag) consistently demonstrate both boundary mechanisms without weakening ceremony for runtime-governing changes.
- compact_delivery_extension_2026_07_18: Pure ungated work retains the Quick Task label. After approved UR, Brownfield Review records Mode/Slice decision, reason, evidence and next depth in the same internal operation; persisted `quick_task` is presented to humans as Compact Delivery without changing the stored or JSON enum. Standalone Mode/Slice Decision is now explicitly fail-closed recovery for interrupted, incomplete or legacy records, reducing routine ceremony without weakening evidence or approval boundaries.
- structured_depth_extension_2026_07_29: Structured Slice and Structured Delivery retain the same gate and approval sequence; proportionality changes only artefact depth. Owner, file, consumer, task and derived-path counts are never standalone thresholds. Scope growth or a newly evidenced full-depth trigger requires re-evaluation before the slice continues, while incomplete facts route to `block` rather than hidden default escalation.

### CG-TASK-TARGET-AUTHORITY

- situation: AGDF could classify repository scope and active runs, but it had no canonical boundary that resolved the user's primary work target before repository activation. An explicit external artefact, the current working directory and repositories used only as evidence could therefore be conflated.
- refs: plugin/meta/contracts/task-target-resolution.md; plugin/meta/agdf-agent-router.md; plugin/skills/gate-check/SKILL.md; plugin/meta/contracts/interaction.md; create-agdf/lib/interaction-presentation.js; .agdf/control/artefacts/task-target-resolution-boundary/SD.md
- evidence: Approved UR, PRD, SD and TP plus the pre-implementation Brownfield Analysis assign one focused target-resolution contract before repository activation and reuse the existing interaction, sync, integrity and eval owners.
- decision: The explicit current-turn work target outranks inherited context and `cwd`; a confirmed target may continue only after unambiguous revalidation. Evidence access never grants mutation or governance authority. Repository-local AGDF activation is derived only from the resolved primary target or an explicit user governance assignment.
- invariants: task target resolution precedes read-only orientation, Scope Classification, mode selection and gate evaluation; unresolved target states forbid repository activation, gate evaluation and mutation; `target_content_mismatch` and `target_unavailable` never fall back to `cwd`, a neighboring file or an evidence repository; a new explicit target ends the old binding; no global target store, second run state, second scope classifier or second presentation owner is created; visible target orientation is non-authorizing and rendered only by the existing interaction-presentation owner.
- risks: model interpretation of continuation can drift, host attachments may lack stable paths and instruction-only surfaces cannot technically enforce every target decision. These risks are controlled by fail-closed ambiguity, visible retry/clarification, deterministic presentation, multi-turn adversarial evals and explicit separation of repository evidence from live-host evidence.
- relationship: `CG-NATIVE-INTERACTION-AUTHORITY` remains the presentation/approval authority; this node owns only work-target and governance-target selection before that interaction layer.
- exit_criteria: canonical and generated surfaces carry the same target-resolution order; Runtime Integrity, renderer tests, single-turn and multi-turn skill evals, package smoke and QA pass; live host attachment behavior remains disclosed unless directly observed.

### CG-NATIVE-INTERACTION-AUTHORITY

- situation: Coding-agent surfaces can present native questions, permission prompts and plan controls, but those host interactions have different authority and must not become interchangeable AGDF approvals.
- refs: plugin/meta/contracts/interaction.md (`Native Interaction Contract`); plugin/skills/gate-check/SKILL.md (focused contract consumer and six orchestration responsibilities); plugin/meta/agdf-plugin.definition.json (`interactions`); .agdf/control/artefacts/surface-native-interactions/SD.md
- evidence: The approved surface-native-interactions design and implementation define one semantic envelope across Codex, Claude Code, OpenCode and exact-text fallback. Runtime integrity, control-state/routing tests, aggregate package smoke, explicit OpenCode missing/allow/deny fixtures, repeated-sync idempotence and Pages check/build passed on 2026-07-14.
- decision: Native controls are presentation adapters only. Host permission, plan approval, timeout/default behavior, hook output and agent messages are not AGDF gate authority.
- invariants: only deliberate user input that passes selected-run, current-gate and durable-artefact validation immediately before persistence can become an AGDF approval; exact text remains a universal fallback; explicit user surface permissions are preserved; no native response writes control state directly. Ready gate classification is independent from adapter availability: `native_attempt_required` is true only after exact-value transport and deliberate-wait capability are proven, while decorated-only, missing, unsafe or conflicting capability fails closed before invocation without changing gate authority.
- risks: host tool schemas and timeout behavior can drift; mitigated by descriptive capability metadata, deterministic integrity/generation tests and fail-closed textual fallback.
- exit_criteria: canonical/runtime and generated-surface tests pass for all declared adapters, OpenCode explicit allow/deny preservation is proven, and QA confirms that supporting live probes are not presented as gate-enforcement evidence.
- rationale_extension_2026_07_16: Gate rationale is presentation-only. The on-demand `Why?` path uses the existing `status` interaction, never adds an approval option, never advances a gate and leaves exact approval authority unchanged; locale parity, length budgets and deterministic retrieval are regression-tested.
- installer_integration_2026_07_16: `installer-output-parity` consumes this node and does not own approval transport metadata or fallback policy. The observed Codex recommended-label constraint remains linked to `agdf-human-decision-surface` live UAT; installer docs state capability dependence and exact-text authority without claiming host-visible button proof.
- ownership_consolidation_2026_07_18: `interaction.md` is the sole normative owner for interaction kinds, locale, adapter capability, deliberate waiting, native fallback, outcomes and validation semantics. Gate-check retains only run/gate selection, durable artefact readiness, verbatim projection consumption, contract-selected deliberate input, same-revision revalidation and exact approval persistence. Positive and independent negative integrity checks enforce those seams without requiring duplicated policy prose.
- operational_status_projection_2026_07_19: `status_card` remains the complete machine/audit data owner and `interaction-presentation.js` now emits additive immutable `status_presentation` Markdown for operational chat. Gate-check CLI, skills and generated surfaces consume it verbatim; no skill-local table or private CLI field selection remains. The primary card stays compact while raw evidence remains in JSON, and missing projection fails closed without affecting approval authority.
- opencode_honesty_extension_2026_07_23: OpenCode `tool.execute.before` does not cover subagent tool calls. AGDF therefore treats the subagent path as audit-only unless the host later provides conforming interception evidence; agents must not route work to a subagent to bypass primary-agent gates, and public capability copy must disclose the limitation. Inactive-repository and plugin/validator-version drift remain observable through failure-tolerant TUI/log warnings, not silent confidence claims.

### CG-CREATE-AGDF-CLI-COMPOSITION

- situation: The published `create-agdf` CLI had accumulated command discovery, parsing, orchestration, installers, scaffolding and control evaluation in one executable entry point, making ownership and isolated verification difficult.
- refs: create-agdf/bin/create-agdf.js; create-agdf/lib/cli/; create-agdf/lib/installers/; create-agdf/lib/scaffold/; create-agdf/lib/control-evaluation/; .agdf/control/artefacts/create-agdf-cli-modularization/SD.md; .agdf/control/artefacts/create-agdf-cli-modularization/OR.md
- evidence: UAT-approved implementation leaves the executable as an 11-line composition root, assigns the approved responsibilities to 16 focused modules, uses a single immutable command registry and explicit handler map, and passes focused modularization, aggregate package smoke, release-bootstrap and Runtime Integrity verification.
- decision: The bin file owns process startup only. CLI application/registry/parser/runtime context, installer adapters, scaffold planning/writing/presentation and control evaluation remain separate acyclic owners under `create-agdf/lib/`; public command, output and exit-code compatibility is preserved at those boundaries.
- invariants: command inventory has one canonical registry; dispatch uses explicit injected handlers; library modules return exit codes instead of terminating the process; subprocess and IO boundaries remain injectable; dependencies point from the composition root toward focused owners without import cycles; generated package assets follow their canonical owners.
- risks: Native Windows execution was unavailable during this run, so Windows assurance remains limited to preserved command construction and cross-platform automated evidence; future commands can regress the boundary if added directly to the bin instead of the registry and focused owner.
- exit_criteria: Any CLI command or responsibility change updates its focused owner, registry where applicable, deterministic tests, package assets and user-facing command routing; the bin remains a thin composition root and the import-cycle check remains green.
- lifecycle_extension_2026_07_16: Additive `status`, `disable` and `uninstall` commands route through the existing registry/parser/application owners. Shared lifecycle result/presentation modules normalize human and JSON results; installer/scaffold adapters retain surface ownership; disable/uninstall use preview, explicit scope and ownership proof rather than adding deletion policy to the composition root.
- local_validator_extension_2026_07_18: Repeated doctor, gate-check, delivery-map and Delivery Path Search execution routes through `validation-handlers.js`, shared by the normal CLI and focused local validator. The Git source plugin contains no generated runtime. `sync-package-assets` builds the exact-version digest-verified payload directly into `create-agdf/generated/plugins/agdf/runtime/`; OpenCode continues to use its exact config-local package. Routine resolution never searches PATH, installs, invokes `npx ...@latest` or contacts a registry, and installer/lifecycle/scaffold modules remain excluded from the validator payload.
- release_built_plugin_distribution_2026_07_18: `create-agdf` package sync/prepack and the publish workflow own complete Codex/Claude plugin composition. `local-marketplace.js` owns one marker-proven cross-platform user-data stage, atomic replacement and rollback; `plugin-installers.js` owns host JSON classification and command sequencing. Only absent, the current owned local source or the exact former AGDF GitHub source may change. Conflicting/unknown registrations fail closed, source `plugin/runtime/` is forbidden, and real host migration remains UAT evidence rather than repository proof.
- opencode_validator_hardening_2026_07_19: The global OpenCode installer fills only missing canonical `question`, `edit`, `bash` and `skill` permissions from the plugin definition while preserving every explicit user decision. The existing config-local validator keeps its stable `.js` path and receives ESM semantics only from an ownership-checked `agdf/package.json`; global-surface preflight runs before mutation, status requires valid owner/surface/module metadata, and clean-install evidence requires empty stderr.

### CG-UX-INTENT-BEFORE-PRD

- situation: UI/UX intent, effective-state semantics, activation, blockers and recovery were previously discoverable only during downstream review, allowing technically functional implementation to precede an explicit product promise.
- refs: plugin/meta/contracts/gate-transition.md; plugin/meta/contracts/quality.md; plugin/skills/ux-intent-definition/SKILL.md; plugin/control/templates/artefacts/PRD.md; plugin/skills/task-plan-review/SKILL.md; plugin/skills/clean-implementation-review/SKILL.md; plugin/skills/code-review/SKILL.md; plugin/skills/qa-gate/SKILL.md; .agdf/control/artefacts/prd-ux-intent-requirements/PRD.md
- evidence: The approved PRD defines 35 observable criteria. The implementation adds one shared post-UR impact classification, a non-authorizing fail-closed UX definition step, mandatory PRD UX prompts, Task Plan Review fidelity evidence and one normalized review-gap contract consumed by Task Plan, Clean, Code and QA. Runtime Integrity passes for 10 skills/16 control files and deterministic evaluations pass 30/30; package and aggregate evidence is recorded in the run artefacts.
- decision: UX intent and other decidable product/design/plan obligations are defined in their authoritative artefact before implementation. Reviews verify those promises, classify concrete implementation/evidence gaps and route missing upstream decisions without creating them. QA consumes normalized findings as the sole final quality decision owner.
- invariants: Greenfield and Brownfield share one `none | low | medium | high` impact vocabulary and routing owner; medium/high and ambiguous low impact fail closed before PRD readiness; product effective-state authority, presentation ownership and technical ownership remain distinct; recoverable transient failures require visible retry semantics; the Quality Contract alone owns `requirements_gap | design_gap | plan_gap | implementation_gap | evidence_gap | emergent_risk` meanings and routing; consumer reviews never duplicate the complete mapping, mutate upstream artefacts or invent missing decisions; `none` is only a fulfilled-row sentinel; open or invalid normalized findings prevent QA pass; visible claims require visible evidence; no UX analysis or review-gap gate exists.
- risks: Repository evidence proves canonical contracts, generated parity and deterministic behavior, not authenticated live-host rendering; application-specific implementations still require direct visible evidence when they claim user-facing behavior.
- exit_criteria: Any UI/UX-impacting run records proportional impact, resolves required UX definition before PRD readiness and maps approved UX criteria through TP and visible evidence. Any review finding uses one normalized type and route, and QA prevents pass for partial, missing, not-verifiable, open or invalid evidence.
- structured_depth_extension_2026_07_29: Structured-depth rationale is visible PRD input, not a second approval authority. Positive decisions expose decisive dimensions, bounded-slice outcome, rejected alternative, evidence and next gate. Unresolved facts expose the missing or conflicting dimensions, evidence owner and recovery action while the existing `block` value prevents later artefacts and implementation.

## Retired Context Nodes

| Node | Reason | Replacement |
|---|---|---|
