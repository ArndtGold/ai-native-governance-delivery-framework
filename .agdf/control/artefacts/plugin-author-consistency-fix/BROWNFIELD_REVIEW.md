# Brownfield Review: Fix Codex Plugin Manifest Author Mismatch Breaking CI

Gate: Brownfield Review
Type: Brownfield Review
Mode: `post_ur_review`
Status: done

## Run

- run_id: plugin-author-consistency-fix
- related_ur: .agdf/control/artefacts/plugin-author-consistency-fix/UR.md
- current_gate: Brownfield Review
- reviewer: agent
- reviewed_at: 2026-07-10

## Objective

Size and route the approved UR, and resolve the open question it flagged: which author value is
actually intended, and which files must change to make `check-runtime-integrity.mjs` pass again.

## Existing-System View

| Area | Existing owner or artefact | Evidence | Impact |
|---|---|---|---|
| Product semantics | none | Metadata-only text field, no behavior change | none |
| Source of truth | `plugin/meta/agdf-plugin.definition.json` (per `.agdf/control/SOT_REGISTRY.md`) | Canonical source that `check-runtime-integrity.mjs` validates all plugin manifests against | high |
| Runtime path | `plugin/.codex-plugin/plugin.json`, `plugin/.claude-plugin/plugin.json` | Both checked line-by-line against the canonical definition's `author.name`/`author.url` in `plugin/scripts/check-runtime-integrity.mjs` (lines 248 and 272) | high |
| UI / UX | none | No UI surface involved | none |
| Persistence / data | none | | none |
| Tests / QA | `plugin/scripts/check-runtime-integrity.mjs`, `.github/workflows/agdf-guardrails.yml` | This is the exact check that currently fails and blocks the whole `verify` job | high |
| Release / operations | `create-agdf/scripts/sync-package-assets.js` | Regenerates `create-agdf/generated/plugins/agdf/**` (gitignored, not committed) from `plugin/` on every `smoke-test`/`prepack` run; must not be hand-edited | medium |

## Reuse And Parallel-Structure Risk

| Finding | Evidence | Risk | Required action |
|---|---|---|---|
| Commit `12f9cd3`'s intent is unambiguous, resolving the UR's open question | Commit message: "Changed author name from 'AI-native Governance & Delivery Framework' to 'Arndt Gold'" — a deliberate, real rename, not an accidental edit | none | Propagate `"Arndt Gold"` forward as the new canonical value; do not revert `plugin/.codex-plugin/plugin.json` back to the org name |
| The UR's own scope statement (reconcile only the two files it named) is incomplete | `check-runtime-integrity.mjs` validates `plugin/.claude-plugin/plugin.json`'s author against the same canonical definition (line 272); it currently still matches the canonical org name, so updating only the canonical file to "Arndt Gold" would fix the Codex check but immediately break the Claude check | warn | Update all three files consistently: `plugin/meta/agdf-plugin.definition.json` (canonical), `plugin/.claude-plugin/plugin.json`, and keep `plugin/.codex-plugin/plugin.json` as-is (already "Arndt Gold") |
| `create-agdf/generated/plugins/agdf/**` also has the old author name | It is gitignored (`.gitignore:28`) and fully regenerated by `create-agdf/scripts/sync-package-assets.js` from `plugin/` | none | Do not hand-edit; regenerate locally via `npm run sync-package-assets` (part of `smoke-test`) as part of verification, do not commit it |
| Root `.claude-plugin/marketplace.json` `owner.name` also says the org name | Not read by `check-runtime-integrity.mjs`'s author checks (`owner` is a different field/concept from plugin `author`) and not causing the CI failure | none | Out of scope per the UR's Non-Goals; noted here for awareness only, not changed |

## Mode / Slice Decision

- decision: quick_task
- required_next_gate: none
- scope_reason: Narrow, local metadata-consistency fix reusing the exact check the codebase already enforces; no new product semantics, architecture, policy or contract change — only reconciling a text field across the canonical definition and its two plugin-manifest mirrors.
- evidence: `check-runtime-integrity.mjs` already defines the exact target state (all three files' `author.name`/`author.url` must match); this is a straightforward reconciliation, not a design decision.
- transparency_note: Quick Task Execution may now update `plugin/meta/agdf-plugin.definition.json` and `plugin/.claude-plugin/plugin.json` to `"Arndt Gold"`, regenerate `create-agdf/generated/` locally for verification (not commit), and re-run the integrity check plus smoke tests.

## PRD / SD Open Questions

| Question | Required gate | Impact |
|---|---|---|
| none | none | none |

## Context Graph Impact

- context_graph_impact: none
- context_graph_refs:
- context_graph_required_action: none
- context_graph_gate_effect: none
- context_graph_evidence: Metadata-only fix; no new durable cross-run knowledge claim.

## Next Permissible Step

- next_allowed_action: Quick Task Execution — update the canonical definition and Claude plugin manifest author fields, regenerate `create-agdf/generated/` locally, verify `check-runtime-integrity.mjs` and smoke tests pass.
- forbidden_until_then: PRD, SD, TP, Brownfield Analysis (not required for quick_task), any change to `owner.name` in `.claude-plugin/marketplace.json` (out of scope).

## Quality Outlook

- quality_outlook: Once fixed and pushed, `agdf-guardrails.yml` should reach and pass the package smoke-test steps, finally providing the CI-level confirmation still outstanding for `gate-state-clarity` and `create-agdf-lib-test-coverage`.
