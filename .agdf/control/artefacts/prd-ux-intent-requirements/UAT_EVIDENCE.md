# UAT Evidence — Define UX Intent Before Implementation

- status: accepted
- run_id: prd-ux-intent-requirements
- based_on: approved QA Report revision 19
- date: 2026-07-19

Revision note: user requested sharper Pages workflow, fidelity and skill representation before UAT.
The revision is implemented and renewed exact QA approval is recorded. UAT may now evaluate the
delivered product contract and public explanation; acceptance remains missing.
Revision 14 subsequently expands product semantics to shared review-gap routing; this evidence is
historical until the revised UR-through-QA chain is complete.
Revision 18 is implemented and QA returns `pass`; revision 19 records the renewed exact QA approval.

Revision 19 exact QA approval is now recorded. UAT may evaluate the complete UX-intent and normalized
review-gap contract; acceptance remains missing.

## Delivered User Outcome

AGDF now defines UX intent before PRD approval when impact requires it and verifies the approved
promise after implementation:

1. post-UR routing records one Greenfield/Brownfield `none | low | medium | high` impact decision;
2. `ux-intent-definition` produces `ready | blocked | not_applicable` structured PRD input and cannot
   approve gates, invent product intent or become a parallel source of truth;
3. the canonical PRD template requires working modes, effective state, state authority, presentation
   ownership, activation, blockers, recovery and transitions;
4. Task Plan Review verifies PRD-to-TP-to-surface UX Intent Fidelity; and
5. QA cannot pass incomplete, not-verifiable or code-only visible-behavior evidence.
6. one Quality Contract defines `requirements_gap`, `design_gap`, `plan_gap`, `implementation_gap`,
   `evidence_gap` and `emergent_risk` with explicit upstream routing;
7. Task Plan, Clean and Code Review classify findings without inventing missing upstream decisions; and
8. QA consumes findings without reclassification and cannot pass open, unknown or contradictory rows.

## Acceptance Evidence

- 12/12 approved TP tasks are fully done.
- 25/25 approved PRD criteria are fulfilled with mapped evidence.
- Runtime Integrity passes for 10 canonical skills and 16 control files.
- Deterministic behavioral evaluations pass 30/30 across normal, boundary and adversarial classes.
- Codex/Claude plugin, Copilot and OpenCode assets are generated by the canonical sync owner; repeat
  synchronization is idempotent.
- Package build/contents, routing, installed-layout/negative integrity and aggregate smoke pass.
- Pages check/build passes and rendered output visibly contains `/ux-intent-definition`,
  `10 Core Workflow Skills`, `10 canonical skills` and `30 behavioral cases`.
- The rendered workflow now shows ten controls with conditional UX Intent Definition between path
  selection and PRD, explicit Greenfield/Brownfield impact routing, PRD product authority, the
  post-code Fidelity chain, fail-closed gap states and the QA blocker.
- The runtime-tree presentation derives all ten folders from canonical skill data instead of a
  manually duplicated subset; local rendered inspection shows no horizontal overflow at 1280x720.
- `CG-UX-INTENT-BEFORE-PRD` records the durable lifecycle invariant.
- Controlled removal of a canonical type and injection of a private consumer mapping both fail
  Runtime Integrity; restored negative tests and aggregate smoke pass.
- Revision 18 completes 7/7 affected tasks and 19/19 total approved tasks with no normalized review finding.

## Deliberate Evidence Boundary

No authenticated Codex, Claude Code, Copilot or OpenCode host session was executed. Repository tests
and deterministic replay prove contracts, generation and package behavior, not live host rendering.
This run does not claim release, publication, installation or live-host UAT evidence.

## UAT Decision

Accepted through exact `Approval: UAT` on 2026-08-20 after selected-run, same-gate, revision-20 and
durable-evidence revalidation. Acceptance retains the disclosed repository-versus-live-host evidence
boundary and does not authorize commit, push, PR, release, publication or installation.
