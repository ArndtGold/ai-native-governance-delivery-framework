# TP: Runtime Contract Modularization

## Tasks

| Task ID | Priority | Description | Evidence | Tests |
|---|---|---|---|---|
| RC-01 | P0 | Create `plugin/meta/contracts/` directory and 7 module files (`gate-transition.md`, `interaction.md`, `modes.md`, `quality.md`, `context-graph.md`, `control-scaffold.md`, `closeout.md`) with exact content extracted from the monolith per PRD module organization | Files exist with correct content | Content matches monolith sections verbatim |
| RC-02 | P0 | Replace `plugin/meta/agdf-runtime-contract.md` with thin ~20-line manifest listing modules | File is ~20 lines, contains module table | No monolith content remains in manifest |
| RC-03 | P0 | Update all 9 plugin skills (`gate-check`, `brownfield-analysis`, `code-review`, `clean-implementation-review`, `task-plan-review`, `qa-gate`, `release-or`, `delivery-path-search`, `delivery-closeout`) to reference specific `../../meta/contracts/<module>.md` instead of `../../meta/agdf-runtime-contract.md` per PRD skill-to-module mapping | No `agdf-runtime-contract.md` references remain in any skill | `grep -r "agdf-runtime-contract" plugin/skills/` returns 0 hits |
| RC-04 | P0 | Update `plugin/meta/agdf-agent-router.md` Runtime Contract section to reference `meta/contracts/` modules | Router references modules | No `agdf-runtime-contract.md` in router (except path mention) |
| RC-05 | P0 | Update `plugin/scripts/check-runtime-integrity.mjs`: add `contractsDir`, `contractModules`, `readAllContracts()` function; replace both `read(runtimeContractPath)` for content checks with `readAllContracts()`; update skill reference check to `../../meta/contracts/`; update gate-check SoT string; update German-pattern check | Checker reads modules, passes with 0 findings | `node plugin/scripts/check-runtime-integrity.mjs` exits 0 |
| RC-06 | P0 | Update `create-agdf/scripts/sync-package-assets.js`: in `syncRuntimeContract()` add loop to copy 7 modules to `generatedSkillsRoot/contracts/` and `generatedOpenCodeRoot/contracts/` with skill-name transforms and locale-path replacement; in `syncSkill()` add `.replaceAll("../../meta/contracts/", "../contracts/")`; in `writeOpenCodeSkill()` add `.replaceAll("../../meta/contracts/", "../../contracts/")` | Generated surfaces contain `contracts/` directory with all 7 modules | `ls generated/.github/skills/contracts/` and `ls generated/.opencode/contracts/` show 7 files |
| RC-07 | P0 | Update `create-agdf/bin/create-agdf.js`: add 7 contract module entries to `codexPluginFiles`, `copilotSkillFiles`, `openCodeFiles`; update `globalOpenCodeConfigPaths`, `assertGlobalOpenCodeSurfaceWritable`, `installOpenCodeGlobalSurface`, `evaluateGlobalOpenCodeSurface` | Installer includes contract modules in all surfaces | smoke-test passes |
| RC-08 | P0 | Update `create-agdf/scripts/runtime-integrity-negative-test.js`: add `interactionContractPath`, replace all `runtimeContractPath` modifications targeting interaction content with `interactionContractPath` | Negative tests pass | `node create-agdf/scripts/runtime-integrity-negative-test.js` exits 0 |
| RC-09 | P1 | Update `create-agdf/scripts/verified-change-test.js`: change `readFileSync` from monolith to `plugin/meta/contracts/modes.md` | Test passes | `node create-agdf/scripts/verified-change-test.js` exits 0 |
| RC-10 | P0 | Update `create-agdf/scripts/smoke-test.js`: update `transitionContractPaths` to point to `contracts/interaction.md` in all three generated surfaces | Smoke test passes | `node create-agdf/scripts/smoke-test.js` exits 0 |
| RC-11 | P1 | Update `.agdf/control/SOT_REGISTRY.md`: Runtime contracts SoT → `plugin/meta/contracts/` | SOT_REGISTRY updated | SoT entry points to contracts directory |
| RC-12 | P1 | Update `.agdf/control/CONTEXT_GRAPH.md`: update node refs in CG-RUN-STATUS-CARD, CG-DELIVERY-PATH-SEARCH, CG-DOCUMENTATION-CEREMONY-BOUNDARY, CG-NATIVE-INTERACTION-AUTHORITY to reference specific modules | CONTEXT_GRAPH refs updated | Reconciliation: resolved |

## Evidence Plan

- `check-runtime-integrity.mjs` — 0 findings
- `runtime-integrity-negative-test.js` — all negative tests pass
- `sync-package-assets.js` — contracts directory in all generated surfaces
- `smoke-test.js` — full smoke passes
- `git diff --check` — no whitespace errors
- Manual verification: module content matches monolith sections verbatim

## Out-of-Scope

- Global OpenCode config at `~/.config/opencode/` — updated on next `npx @agdf/cli@latest opencode` run
- Generated `create-agdf/generated/` directory — regenerated by sync script, not manually edited
