name: Publish AGDF packages

on:
  push:
    tags:
      - "agdf-v*"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Verify release tag matches package versions
        shell: bash
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          CREATE_AGDF_VERSION="$(node -p "JSON.parse(require('node:fs').readFileSync('./create-agdf/package.json', 'utf8')).version")"
          AGDF_CLI_VERSION="$(node -p "JSON.parse(require('node:fs').readFileSync('./agdf/package.json', 'utf8')).version")"
          AGDF_CLI_CREATE_AGDF_VERSION="$(node -p "JSON.parse(require('node:fs').readFileSync('./agdf/package.json', 'utf8')).dependencies['create-agdf']")"
          EXPECTED_TAG="agdf-v${CREATE_AGDF_VERSION}"

          if [ "${TAG_NAME}" != "${EXPECTED_TAG}" ]; then
            echo "Tag ${TAG_NAME} does not match ${EXPECTED_TAG}" >&2
            exit 1
          fi

          if [ "${AGDF_CLI_VERSION}" != "${CREATE_AGDF_VERSION}" ]; then
            echo "@agdf/cli version ${AGDF_CLI_VERSION} does not match create-agdf version ${CREATE_AGDF_VERSION}" >&2
            exit 1
          fi

          if [ "${AGDF_CLI_CREATE_AGDF_VERSION}" != "${CREATE_AGDF_VERSION}" ]; then
            echo "@agdf/cli depends on create-agdf@${AGDF_CLI_CREATE_AGDF_VERSION}, expected ${CREATE_AGDF_VERSION}" >&2
            exit 1
          fi

      - name: Verify AGDF runtime and plugin metadata
        run: node plugin/scripts/check-runtime-integrity.mjs

      - name: Run create-agdf smoke test
        run: npm --prefix create-agdf run smoke-test

      - name: Run @agdf/cli smoke test
        run: npm --prefix agdf run smoke-test

      - name: Verify create-agdf package contents
        working-directory: create-agdf
        run: npm pack --dry-run

      - name: Verify @agdf/cli package contents
        working-directory: agdf
        run: npm pack --dry-run

  publish:
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Publish create-agdf to npm
        working-directory: create-agdf
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance

      - name: Publish @agdf/cli to npm
        working-directory: agdf
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --access public

      - name: Wait for npm package readiness
        shell: bash
        run: |
          VERSION="$(node -p "JSON.parse(require('node:fs').readFileSync('./create-agdf/package.json', 'utf8')).version")"
          PACKAGES=("create-agdf" "@agdf/cli")
          MAX_ATTEMPTS=20
          SLEEP_SECONDS=15
          NPM_ERROR_LOG="$(mktemp)"
          trap 'rm -f "${NPM_ERROR_LOG}"' EXIT

          for PACKAGE in "${PACKAGES[@]}"; do
            LAST_ERROR=""
            for ATTEMPT in $(seq 1 "${MAX_ATTEMPTS}"); do
              if RESULT="$(npm view "${PACKAGE}@${VERSION}" version --json 2>"${NPM_ERROR_LOG}")"; then
                RESOLVED_VERSION="$(node -e "const value = JSON.parse(process.argv[1]); process.stdout.write(String(value));" "${RESULT}")"
                if [ "${RESOLVED_VERSION}" = "${VERSION}" ]; then
                  echo "${PACKAGE}@${VERSION} is resolvable on npm."
                  LAST_ERROR=""
                  break
                fi
                LAST_ERROR="resolved ${RESOLVED_VERSION}, expected ${VERSION}"
              else
                LAST_ERROR="$(cat "${NPM_ERROR_LOG}")"
              fi

              if [ "${ATTEMPT}" -lt "${MAX_ATTEMPTS}" ]; then
                sleep "${SLEEP_SECONDS}"
              fi
            done

            if [ -n "${LAST_ERROR}" ]; then
              echo "Timed out waiting for ${PACKAGE}@${VERSION} after ${MAX_ATTEMPTS} attempts. Last error: ${LAST_ERROR}" >&2
              exit 1
            fi
          done
