{
    "hooks": {
        "SessionStart": [
            {
                "hooks": [
                    {
                        "type":         "command",
                        "command":      "bash -lc 'hook=\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}/hooks/session-start.sh\"; if [ -f \"$hook\" ]; then exec bash \"$hook\"; fi; hook=\"$PWD/plugins/agdf/hooks/session-start.sh\"; if [ -f \"$hook\" ]; then exec bash \"$hook\"; fi; exit 0'",
                        "statusMessage": "Loading AGDF..."
                    }
                ]
            }
        ]
    }
}
